{
  "name": "hubot-time-until",
  "version": "1.0.0",
  "description": "Get the number of days until a date",
  "main": "index.js",
  "scripts": {
    "shell": "hubot --adapter shell --disable-httpd",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "hubot",
    "hubot-scripts"
  ],
  "devDependencies": {
    "coffee-script": "^1.9.3",
    "hubot": "^2.16.0"
  },
  "dependencies": {
    "moment": "^2.19.3"
  }
}
